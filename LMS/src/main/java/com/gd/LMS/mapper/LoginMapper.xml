<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gd.LMS.mapper.LoginMapper">

<select id="selectLogin" parameterType="com.gd.LMS.vo.Member"
	resultType="com.gd.LMS.vo.Member">
	SELECT
		member_id memberId,
		member_pw memberPw,
		member_type memberType,
		active
	FROM member
	WHERE 
		member_id = #{memberId} 
	AND 
		member_pw = #{memberPw}
</select>


</mapper>